#include <bits/stdc++.h>
using namespace std;

int main() {
	int total,convidados;
	cin >> total >> convidados;
	int hierarquia[n+1];
	vector<set<int>> chefe(n);
	for (int i = 2; i <= n; i++) {
		cin >> chefe(i).insert();
	}
	return 0;
}
